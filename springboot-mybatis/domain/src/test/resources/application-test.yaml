spring:
  http:
    multipart:
      enabled: true
      file-size-threshold: 0
      max-file-size: 100MB
      max-request-size: 100MB
      location:
  mvc:
    async:
      request-timeout: 10000
  datasource:
    initialize: true
    platform: h2

logging:
  level:
    org.scripton: DEBUG
    org.springframework: DEBUG
    org.mybatis: DEBUG